diff --git a/dist/NVConstants.d.ts b/dist/NVConstants.d.ts
index 7e65858379bcb0eb58cde7ef98137ce8716e7bfd..65287eeebcb502e0a7c029b7e0daa7ed36a3e4bf 100644
--- a/dist/NVConstants.d.ts
+++ b/dist/NVConstants.d.ts
@@ -16,7 +16,8 @@ export declare enum DRAG_MODE {
     roiSelection = 6,
     angle = 7,
     crosshair = 8,
-    windowing = 9
+    windowing = 9,
+    crosshairPan = 10
 }
 export declare enum SHOW_RENDER {
     NEVER = 0,
diff --git a/dist/NVControlBase-Cq7vd-TD.js b/dist/NVControlBase-Cq7vd-TD.js
index 233eaa4b018793ea685dedebbd73c0c8a6417728..4ee260e4a9fca68641b7c4b5fd6d0ec13f0f7883 100644
--- a/dist/NVControlBase-Cq7vd-TD.js
+++ b/dist/NVControlBase-Cq7vd-TD.js
@@ -1836,7 +1836,7 @@ function $e(t, e, n = !1) {
   ]);
 }
 const ln = 6, Fp = [0, 0, 0, 2];
-var We = /* @__PURE__ */ ((t) => (t[t.MIN_TO_MAX = 0] = "MIN_TO_MAX", t[t.ZERO_TO_MAX_TRANSPARENT_BELOW_MIN = 1] = "ZERO_TO_MAX_TRANSPARENT_BELOW_MIN", t[t.ZERO_TO_MAX_TRANSLUCENT_BELOW_MIN = 2] = "ZERO_TO_MAX_TRANSLUCENT_BELOW_MIN", t))(We || {}), ge = /* @__PURE__ */ ((t) => (t[t.none = 0] = "none", t[t.contrast = 1] = "contrast", t[t.measurement = 2] = "measurement", t[t.pan = 3] = "pan", t[t.slicer3D = 4] = "slicer3D", t[t.callbackOnly = 5] = "callbackOnly", t[t.roiSelection = 6] = "roiSelection", t[t.angle = 7] = "angle", t[t.crosshair = 8] = "crosshair", t[t.windowing = 9] = "windowing", t))(ge || {}), nn = /* @__PURE__ */ ((t) => (t[t.NEVER = 0] = "NEVER", t[t.ALWAYS = 1] = "ALWAYS", t[t.AUTO = 2] = "AUTO", t))(nn || {});
+var We = /* @__PURE__ */ ((t) => (t[t.MIN_TO_MAX = 0] = "MIN_TO_MAX", t[t.ZERO_TO_MAX_TRANSPARENT_BELOW_MIN = 1] = "ZERO_TO_MAX_TRANSPARENT_BELOW_MIN", t[t.ZERO_TO_MAX_TRANSLUCENT_BELOW_MIN = 2] = "ZERO_TO_MAX_TRANSLUCENT_BELOW_MIN", t))(We || {}), ge = /* @__PURE__ */ ((t) => (t[t.none = 0] = "none", t[t.contrast = 1] = "contrast", t[t.measurement = 2] = "measurement", t[t.pan = 3] = "pan", t[t.slicer3D = 4] = "slicer3D", t[t.callbackOnly = 5] = "callbackOnly", t[t.roiSelection = 6] = "roiSelection", t[t.angle = 7] = "angle", t[t.crosshair = 8] = "crosshair", t[t.windowing = 9] = "windowing", t[t.crosshairPan = 10] = "crosshairPan", t))(ge || {}), nn = /* @__PURE__ */ ((t) => (t[t.NEVER = 0] = "NEVER", t[t.ALWAYS = 1] = "ALWAYS", t[t.AUTO = 2] = "AUTO", t))(nn || {});
 const Pr = Object.freeze({
   NIFTI_INTENT_NONE: 0,
   NIFTI_INTENT_CORREL: 2,
@@ -2350,10 +2350,18 @@ function Lb(t) {
       else {
         const b = d.sliceType, g = c[b];
         if (!g?.screen) continue;
-        const v = g.screen.fovMM, w = Math.min(T / v[0], _ / v[1]), E = v[0] * w, A = v[1] * w, M = Dr(b, s), P = {
-          leftTopWidthHeight: [p + (T - E) / 2, y + (_ - A) / 2, E, A],
+        const C = xo(g.screen);
+        if (d.squareCropFraction) {
+          const K = Math.min(Math.abs(C.fovMM[0]), Math.abs(C.fovMM[1])) * d.squareCropFraction;
+          for (let J = 0; J < 2; J++) {
+            const ne = (C.mnMM[J] + C.mxMM[J]) / 2;
+            C.mnMM[J] = ne - K / 2, C.mxMM[J] = ne + K / 2, C.fovMM[J] = K;
+          }
+        }
+        const v = C.fovMM, w = Math.min(T / v[0], _ / v[1]), E = v[0] * w, A = v[1] * w, M = Dr(b, s), F = Math.min(T, _), P = {
+          leftTopWidthHeight: d.squareCropFraction ? [p + (T - F) / 2, y + (_ - F) / 2, F, F] : [p + (T - E) / 2, y + (_ - A) / 2, E, A],
           axCorSag: b,
-          screen: xo(g.screen),
+          screen: C,
           azimuth: M.azimuth,
           elevation: M.elevation
         };
@@ -12279,63 +12287,18 @@ function xS(t) {
       return { lengthMM: n, label: `${n} mm` };
   return null;
 }
-function Yx(t, e, n, i, r) {
-  const s = pS(t);
-  if (!s?.screen || !s.leftTopWidthHeight) return null;
-  const o = jx[s.axCorSag];
-  if (!o) return null;
-  const a = s.screen.fovMM, l = Math.abs(a[o[0]]), [c, h, d, u] = s.leftTopWidthHeight, f = xS(l);
-  if (!f) return null;
-  const m = d / l, x = f.lengthMM * m, p = 5, y = x / p, T = 4, _ = 8, b = c + d - _, g = b - x, v = h + u - _ - T, w = [];
-  for (let S = 0; S < p; S++)
-    if (S % 2 === 0) {
-      const F = g + S * y, U = g + (S + 1) * y;
-      w.push(n(F, v, U, v, T, i));
-    }
-  const E = 1, A = v - T / 2, M = v + T / 2;
-  w.push(
-    n(
-      g,
-      A,
-      b,
-      A,
-      E,
-      i
-    )
-  ), w.push(
-    n(
-      g,
-      M,
-      b,
-      M,
-      E,
-      i
-    )
-  ), w.push(
-    n(
-      g,
-      A,
-      g,
-      M,
-      E,
-      i
-    )
-  ), w.push(
-    n(
-      b,
-      A,
-      b,
-      M,
-      E,
-      i
-    )
-  );
-  for (let S = 1; S < p; S++) {
-    const F = g + S * y;
-    w.push(n(F, A, F, M, E, i));
-  }
-  const P = g + x / 2, D = v + T / 2 + 2, I = [], R = e(f.label, P, D, 0.8, i, 0, 0);
-  return R.count > 0 && I.push(R), { lines: w, labels: I };
+function Yx(t, e, n, i, r, s = 1, o) {
+  const a = pS(t);
+  if (!a?.screen || !a.leftTopWidthHeight) return null;
+  const l = jx[a.axCorSag];
+  if (!l) return null;
+  const c = a.screen.fovMM, h = Number.isFinite(s) && s > 0 ? s : 1, d = Math.abs(c[l[0]]) / h, [u, f, m, x] = a.leftTopWidthHeight, p = m / d;
+  if (!Number.isFinite(p) || p <= 0) return null;
+  const y = Math.min(160, Math.max(60, m * 0.2)), T = y / p, _ = T >= 10 ? `${Number((T / 10).toPrecision(2))} cm` : T >= 1 ? `${Number(T.toPrecision(2))} mm` : `${Number((T * 1e3).toPrecision(2))} um`, b = o ? 16 : 8, g = o ? o[0] - b : u + m - b, v = g - y, w = o ? o[1] - 48 : f + x - b - 2, E = [0, 0, 0, 0.85], A = [1, 1, 1, 1], M = [
+    n(v, w, g, w, 5, E),
+    n(v, w, g, w, 2, A)
+  ], P = [], D = e(_, v + y / 2, w + 4, 1, A, 0.5, 0, E);
+  return D.count > 0 && P.push(D), { lines: M, labels: P };
 }
 function Zx(t, e, n = [1, 1, 1]) {
   if (e < 1)
@@ -12639,16 +12602,35 @@ function yS(t, e, n, i = {}) {
           R($, N, s);
         }
     return y(I, P);
+  }, H = (M, P) => {
+    let D = M;
+    if (e.reserveBounds?.length) {
+      const I = (R) => e.reserveBounds.some((S) => R.voxelOrigin.every((F, U) => F < S.max[U] && F + R.voxelDims[U] > S.min[U]));
+      for (let R = s; R > P; R--) {
+        const S = [];
+        for (const F of D)
+          (F.sourceLevel ?? 0) === R && I(F) ? p(F, S) : S.push(F);
+        D = S;
+      }
+      return D;
+    }
+    for (let I = P; I > o; I--) {
+      const R = D.findIndex((S) => (S.sourceLevel ?? 0) === I && S.voxelOrigin.every((F, U) => (e.reserveCenter ?? e.center)[U] >= F && (e.reserveCenter ?? e.center)[U] < F + S.voxelDims[U]));
+      if (R < 0) break;
+      const S = [];
+      p(D[R], S), D = [...D.slice(0, R), ...S, ...D.slice(R + 1)];
+    }
+    return D;
   }, _ = i.budgetBytes && i.budgetBytes > 0 ? i.budgetBytes : 0, b = i.maxBricks && i.maxBricks > 0 ? i.maxBricks : Number.POSITIVE_INFINITY, g = (M) => M.reduce(
     (P, D) => P + D.texDims[0] * D.texDims[1] * D.texDims[2] * 8,
     0
   ), v = (M) => _ > 0 && g(M) > _ || M.length > b;
-  let w = d, E = o, A = T(w, E);
+  let w = d, E = o, A = H(T(w, E), E);
   if (_ > 0 || b !== Number.POSITIVE_INFINITY) {
     for (let M = 0; M < 16 && v(A); M++)
-      w /= 1.6, A = T(w, E);
+      w /= 1.6, A = H(T(w, E), E);
     for (; v(A) && E < s; )
-      E++, A = T(w, E);
+      E++, A = H(T(w, E), E);
     if (A.length > b) {
       const M = Math.max(
         1,
@@ -12656,7 +12638,7 @@ function yS(t, e, n, i = {}) {
       );
       let P = 1;
       for (; A.length > b && P < M; )
-        P = Math.min(M, P * 2), A = T(w, s, P);
+        P = Math.min(M, P * 2), A = H(T(w, s, P), s);
     }
   }
   return {
@@ -15426,7 +15408,7 @@ let PA = class extends ze {
 };
 class y2 {
   constructor(e, n, i) {
-    this._resident = /* @__PURE__ */ new Map(), this._uploadQueue = [], this._inFlightUploads = /* @__PURE__ */ new Set(), this._residentBytes = 0, this._frame = 0, this._generation = 0, this._chunkCount = e, this._budgetBytes = n, this._hooks = i;
+    this._resident = /* @__PURE__ */ new Map(), this._uploadQueue = /* @__PURE__ */ new Map(), this._inFlightUploads = /* @__PURE__ */ new Set(), this._residentBytes = 0, this._frame = 0, this._generation = 0, this._staleDropped = 0, this._chunkCount = e, this._budgetBytes = n, this._hooks = i;
   }
   /** Total chunks in the volume's plan, resident or not. */
   get chunkCount() {
@@ -15448,7 +15430,7 @@ class y2 {
     }
     this._resident.clear();
     for (const [r, s] of i) this._resident.set(r, s);
-    this._uploadQueue.length = 0, this._inFlightUploads.clear(), this._chunkCount = n, this._generation++;
+    this._uploadQueue.clear(), this._inFlightUploads.clear(), this._chunkCount = n, this._generation++;
   }
   /**
    * Monotonic plan-generation counter, bumped on every `remap`. A backend upload
@@ -15462,6 +15444,8 @@ class y2 {
   /** Advance the LRU clock. Call once per frame before consuming chunks. */
   beginFrame() {
     this._frame++;
+    for (const [e, n] of this._uploadQueue)
+      this._isStale(n) && this._dropQueued(e);
   }
   /** Current LRU frame counter. */
   get frame() {
@@ -15516,6 +15500,10 @@ class y2 {
   get residentCount() {
     return this._resident.size;
   }
+  /** Current resident chunk indices, in admission order. */
+  residentIndices() {
+    return [...this._resident.keys()];
+  }
   /** True once every chunk in the plan is resident. */
   get isFullyResident() {
     return this._resident.size === this.chunkCount;
@@ -15551,11 +15539,17 @@ class y2 {
       n.lastFrame = this._frame;
       return;
     }
-    this._inFlightUploads.has(e) || this._uploadQueue.includes(e) || (this._uploadQueue.push(e), this._hooks.prefetch?.(e));
+    if (this._inFlightUploads.has(e)) return;
+    const i = this._uploadQueue.get(e);
+    i !== this._frame && (i !== void 0 ? (this._uploadQueue.delete(e), this._uploadQueue.set(e, this._frame)) : (this._uploadQueue.set(e, this._frame), this._hooks.prefetch?.(e)));
   }
   /** Number of chunks queued for upload but not yet resident. */
   get pendingUploadCount() {
-    return this._uploadQueue.length;
+    return this._uploadQueue.size;
+  }
+  /** Number of queued chunks retired after the view stopped requesting them. */
+  get staleDropCount() {
+    return this._staleDropped;
   }
   /** Number of chunks removed from the queue and currently being uploaded. */
   get inFlightUploadCount() {
@@ -15567,12 +15561,7 @@ class y2 {
    * prefetch the upcoming working set ahead of the serial upload pump.
    */
   peekPendingUploads(e) {
-    const n = [], i = Math.max(0, e);
-    for (const r of this._uploadQueue) {
-      if (n.length >= i) break;
-      this._resident.has(r) || this._inFlightUploads.has(r) || n.push(r);
-    }
-    return n;
+    return this._drainOrder(e);
   }
   /**
    * Remove and return up to `max` queued chunk indices, oldest first, for the
@@ -15580,12 +15569,9 @@ class y2 {
    * the backend either `admit`s them or calls `failUpload`.
    */
   takePendingUploads(e) {
-    const n = [], i = Math.max(0, e);
-    for (; n.length < i && this._uploadQueue.length > 0; ) {
-      const r = this._uploadQueue.shift();
-      if (r === void 0) break;
-      this._resident.has(r) || this._inFlightUploads.has(r) || (this._inFlightUploads.add(r), n.push(r));
-    }
+    const n = this._drainOrder(e);
+    for (const i of n)
+      this._uploadQueue.delete(i), this._inFlightUploads.add(i);
     return n;
   }
   /**
@@ -15625,11 +15611,22 @@ class y2 {
   /** Destroy every resident chunk's GPU resources and reset all state. */
   destroy() {
     for (const e of this._resident.values()) this._hooks.destroy(e.chunk);
-    this._resident.clear(), this._residentBytes = 0, this._uploadQueue.length = 0, this._inFlightUploads.clear();
+    this._resident.clear(), this._residentBytes = 0, this._uploadQueue.clear(), this._inFlightUploads.clear(), this._generation++;
   }
   _removeQueuedUpload(e) {
-    for (let n = this._uploadQueue.length - 1; n >= 0; n--)
-      this._uploadQueue[n] === e && this._uploadQueue.splice(n, 1);
+    this._uploadQueue.delete(e);
+  }
+  _drainOrder(e) {
+    const n = Math.max(0, e), i = [], r = [];
+    for (const [s, o] of this._uploadQueue)
+      this._resident.has(s) || this._inFlightUploads.has(s) ? this._uploadQueue.delete(s) : this._isStale(o) ? this._dropQueued(s) : o === this._frame ? i.push(s) : r.push(s);
+    return [...i, ...r].slice(0, n);
+  }
+  _isStale(e) {
+    return this._frame - e > 1;
+  }
+  _dropQueued(e) {
+    this._uploadQueue.delete(e), this._staleDropped++, this._hooks.cancel?.(e);
   }
 }
 function g2(t, e, n, i) {
@@ -16094,8 +16091,8 @@ function Oa(t, e, n, i = () => !0) {
     throw new Error(
       "orientChunkedGL: source is non-RAS but missing RAS mapping"
     );
-  const x = /* @__PURE__ */ new Map();
-  function p(b) {
+  const x = /* @__PURE__ */ new Map(), rawChunks = /* @__PURE__ */ new Map();
+  function p(b, C) {
     const g = n.chunks[b];
     if (!g)
       return Promise.reject(
@@ -16109,7 +16106,8 @@ function Oa(t, e, n, i = () => !0) {
           desc: g,
           plan: n,
           datatypeCode: s,
-          bytesPerVoxel: a
+          bytesPerVoxel: a,
+          signal: C
         })
       ).then((E) => GA(E, v));
     const w = u || !f || !m ? jt(
@@ -16130,22 +16128,29 @@ function Oa(t, e, n, i = () => !0) {
   }
   function y(b) {
     if (!r) return p(b);
+    const w = rawChunks.get(b);
+    if (w) return Promise.resolve(w);
     const g = x.get(b);
-    if (g) return g;
-    const v = p(b);
-    return x.set(b, v), v.catch(() => {
-      x.get(b) === v && x.delete(b);
-    }), v;
+    if (g) return g.promise;
+    const v = new AbortController(), E = p(b, v.signal), A = { promise: E, controller: v };
+    return x.set(b, A), E.catch(() => {
+      x.get(b) === A && x.delete(b);
+    }), E;
   }
   function T(b) {
-    r && (x.has(b) || x.size >= zA || y(b));
+    r && !x.has(b) && x.size < zA && y(b).catch(() => {
+    });
+  }
+  function C(b) {
+    const g = x.get(b);
+    g && (x.delete(b), g.controller.abort());
   }
   async function _(b) {
     const g = n.chunks[b];
     if (!g)
       throw new Error(`orientChunkedGL: chunk index ${b} out of range`);
     const v = await y(b);
-    x.delete(b);
+    x.delete(b), r && rawChunks.set(b, v);
     const w = o ? hA(t, f2(v, s), g.texDims) : _A(t, v, s, g.texDims, e), E = [
       g.texDims[0],
       g.texDims[1],
@@ -16153,7 +16158,18 @@ function Oa(t, e, n, i = () => !0) {
     ], A = i(), M = A ? zi(t, w, E) : VA(t, E);
     return { volumeTexture: w, volumeGradientTexture: M, desc: g, hasGradient: A };
   }
-  return { uploadChunk: _, prefetchChunk: T, dispose: () => x.clear() };
+  return {
+    uploadChunk: _,
+    prefetchChunk: T,
+    cancelChunk: C,
+    refreshWindow: () => {
+    },
+    releaseChunk: (b) => rawChunks.delete(b),
+    dispose: () => {
+      for (const b of x.values()) b.controller.abort();
+      x.clear(), rawChunks.clear();
+    }
+  };
 }
 function $A(t, e) {
   if (e)
@@ -16672,6 +16688,9 @@ function qA(t) {
   const [e, n, i] = t.desc.texDims;
   return e * n * i * 8;
 }
+function chunkWindowKey(t) {
+  return `${t.calMin}|${t.calMax}`;
+}
 function $a(t, e) {
   const n = t.chunks[e], [i, r, s] = t.volumeDims, [o, a, l] = n.texDims, c = t.levelDims?.[n.sourceLevel ?? 0] ?? t.volumeDims;
   return {
@@ -16884,9 +16903,22 @@ let jA = class extends ze {
         `Volume ${n.name} tiles into ${a.chunks.length} chunks, exceeding the per-tile limit of ${Li}. Use a coarser pyramid level or crop the volume.`
       );
     const h = n.url || n.name, d = h ? this._texCache.get(h) : void 0;
-    if (d && d.kind === "chunked")
-      return d.volume = n, d;
-    d && this._destroyTexEntry(e, d);
+    if (d && d.kind === "chunked") {
+      if (d.plan === a) {
+        d.volume = n;
+        const m = chunkWindowKey(n);
+        if (d.windowKey !== m) {
+          const x = d.manager.residentIndices();
+          d.windowKey = m, d.uploader.refreshWindow(), d.manager.destroy();
+          const p = x[0] ?? 0, y = d.manager.generation, T = await d.uploader.uploadChunk(p);
+          d.manager.generation === y ? d.manager.admit(p, T) : (d.manager.discardUpload(p, T), d.uploader.releaseChunk(p));
+          for (const v of x)
+            v !== p && d.manager.requestUpload(v);
+        }
+        return d;
+      }
+      this._destroyTexEntry(e, d), h && this._texCache.delete(h);
+    } else d && (this._destroyTexEntry(e, d), h && this._texCache.delete(h));
     const u = {
       kind: "chunked",
       volume: n,
@@ -16897,7 +16929,8 @@ let jA = class extends ze {
         a,
         () => this.gradientAmount > 0
       ),
-      plan: a
+      plan: a,
+      windowKey: chunkWindowKey(n)
     };
     u.manager = new y2(
       a.chunks.length,
@@ -16905,7 +16938,9 @@ let jA = class extends ze {
       {
         bytesOf: qA,
         destroy: (m) => $A(e, [m]),
-        prefetch: (m) => u.uploader.prefetchChunk(m)
+        onEvict: (m) => u.uploader.releaseChunk(m),
+        prefetch: (m) => u.uploader.prefetchChunk(m),
+        cancel: (m) => u.uploader.cancelChunk(m)
       }
     );
     const f = await u.uploader.uploadChunk(0);
@@ -17211,7 +17246,7 @@ let jA = class extends ze {
             const h = await l.uploadChunk(a);
             s.manager.generation === c ? (h.hasGradient || (this._uploadedUnlit = !0), s.manager.admit(a, h)) : s.manager.discardUpload(a, h);
           } catch (h) {
-            s.manager.failUpload(a), V.error("chunk upload failed", h);
+            s.manager.failUpload(a), h?.name !== "AbortError" && V.error("chunk upload failed", h);
             continue;
           }
           e = !0, n++, r = !0;
@@ -19280,7 +19315,9 @@ class _2 {
           (O, $, N, q, j, Y, K, J) => this.fontRenderer.buildText(O, $, N, q, j, Y, K, J),
           Pe,
           n.ui.fontColor,
-          n.scene.backgroundColor
+          n.scene.backgroundColor,
+          n.scene.pan2Dxyzmm[3],
+          [d, u]
         );
         U && (c.push(...U.labels), E.push(...U.lines));
       }
@@ -22835,8 +22872,8 @@ async function Ja(t, e, n, i = () => !0) {
   ) : null, f = r ? !0 : m2(e), m = e.img2RASstart, x = e.img2RASstep;
   if (!r && !f && (!x || !m))
     throw new Error("orientChunked: source is non-RAS but missing RAS mapping");
-  const p = /* @__PURE__ */ new Map();
-  function y(v) {
+  const p = /* @__PURE__ */ new Map(), rawChunks = /* @__PURE__ */ new Map();
+  function y(v, C) {
     const w = n.chunks[v];
     if (!w)
       return Promise.reject(
@@ -22850,7 +22887,8 @@ async function Ja(t, e, n, i = () => !0) {
           desc: w,
           plan: n,
           datatypeCode: s,
-          bytesPerVoxel: l
+          bytesPerVoxel: l,
+          signal: C
         })
       ).then((M) => q5(M, E));
     const A = f || !m || !x ? jt(
@@ -22871,22 +22909,29 @@ async function Ja(t, e, n, i = () => !0) {
   }
   function T(v) {
     if (!r) return y(v);
+    const A = rawChunks.get(v);
+    if (A) return Promise.resolve(A);
     const w = p.get(v);
-    if (w) return w;
-    const E = y(v);
-    return p.set(v, E), E.catch(() => {
-      p.get(v) === E && p.delete(v);
-    }), E;
+    if (w) return w.promise;
+    const E = new AbortController(), M = y(v, E.signal), P = { promise: M, controller: E };
+    return p.set(v, P), M.catch(() => {
+      p.get(v) === P && p.delete(v);
+    }), M;
   }
   function _(v) {
-    r && (p.has(v) || p.size >= H5 || T(v));
+    r && !p.has(v) && p.size < H5 && T(v).catch(() => {
+    });
+  }
+  function C(v) {
+    const w = p.get(v);
+    w && (p.delete(v), w.controller.abort());
   }
   async function b(v) {
     const w = n.chunks[v];
     if (!w)
       throw new Error(`orientChunked: chunk index ${v} out of range`);
     const E = await T(v);
-    p.delete(v);
+    p.delete(v), r && rawChunks.set(v, E);
     let A;
     if (o)
       A = t.createTexture({
@@ -22952,9 +22997,19 @@ async function Ja(t, e, n, i = () => !0) {
     };
   }
   function g() {
-    p.clear(), c && (c.uniformBuffer.destroy(), c.colormapTexture.destroy(), c.hasNegativeColormap && c.negativeColormapTexture.destroy(), c.modPlaceholder.destroy());
+    for (const v of p.values()) v.controller.abort();
+    p.clear(), rawChunks.clear(), c && (c.uniformBuffer.destroy(), c.colormapTexture.destroy(), c.hasNegativeColormap && c.negativeColormapTexture.destroy(), c.modPlaceholder.destroy());
   }
-  return { uploadChunk: b, prefetchChunk: _, dispose: g };
+  return {
+    uploadChunk: b,
+    prefetchChunk: _,
+    cancelChunk: C,
+    refreshWindow: () => {
+      c && X5(t, c.uniformBuffer, e);
+    },
+    releaseChunk: (v) => rawChunks.delete(v),
+    dispose: g
+  };
 }
 function Y5(t) {
   if (t)
@@ -23753,9 +23808,22 @@ class t4 extends ze {
         `Volume ${n.name} tiles into ${l.chunks.length} chunks, exceeding the per-tile limit of ${en}. Use a coarser pyramid level or crop the volume.`
       );
     const d = n.url || n.name, u = d ? this._texCache.get(d) : void 0;
-    if (u && u.kind === "chunked")
-      return u.volume = n, u;
-    u && this._destroyTexEntry(u);
+    if (u && u.kind === "chunked") {
+      if (u.plan === l) {
+        u.volume = n;
+        const p = chunkWindowKey(n);
+        if (u.windowKey !== p) {
+          const y = u.manager.residentIndices();
+          u.windowKey = p, u.uploader.refreshWindow(), u.manager.destroy(), u.bindGroups.fill(null), this._invalidateBindGroupCache();
+          const T = y[0] ?? 0, v = u.manager.generation, _ = await u.uploader.uploadChunk(T);
+          u.manager.generation === v ? u.manager.admit(T, _) : (u.manager.discardUpload(T, _), u.uploader.releaseChunk(T));
+          for (const g of y)
+            g !== T && u.manager.requestUpload(g);
+        }
+        return u;
+      }
+      this._destroyTexEntry(u), d && this._texCache.delete(d);
+    } else u && (this._destroyTexEntry(u), d && this._texCache.delete(d));
     const f = await Ja(
       e,
       n,
@@ -23767,7 +23835,8 @@ class t4 extends ze {
       manager: void 0,
       uploader: f,
       plan: l,
-      bindGroups: l.chunks.map(() => null)
+      bindGroups: l.chunks.map(() => null),
+      windowKey: chunkWindowKey(n)
     };
     m.manager = new y2(
       l.chunks.length,
@@ -23776,9 +23845,10 @@ class t4 extends ze {
         bytesOf: e4,
         destroy: (p) => Y5([p]),
         onEvict: (p) => {
-          m.bindGroups[p] = null, r?.(p);
+          m.uploader.releaseChunk(p), m.bindGroups[p] = null, r?.(p);
         },
         prefetch: (p) => m.uploader.prefetchChunk(p),
+        cancel: (p) => m.uploader.cancelChunk(p),
         onAdmit: r
       }
     );
@@ -24093,7 +24163,7 @@ class t4 extends ze {
             const h = await l.uploadChunk(a);
             s.manager.generation === c ? (h.hasGradient || (this._uploadedUnlit = !0), s.manager.admit(a, h)) : s.manager.discardUpload(a, h);
           } catch (h) {
-            s.manager.failUpload(a), V.error("chunk upload failed", h);
+            s.manager.failUpload(a), h?.name !== "AbortError" && V.error("chunk upload failed", h);
             continue;
           }
           e = !0, n++, r = !0;
@@ -26847,7 +26917,9 @@ class S2 {
           (q, j, Y, K, J, ne, ce, z) => this.fontRenderer.buildText(q, j, Y, K, J, ne, ce, z),
           Pe,
           e.ui.fontColor,
-          e.scene.backgroundColor
+          e.scene.backgroundColor,
+          e.scene.pan2Dxyzmm[3],
+          [u, f]
         );
         N && (h.push(...N.labels), P.push(...N.lines));
       }
@@ -32599,6 +32671,11 @@ function to(t, e) {
   }
   return i.length > 0 && n.push({ ...e, polygons: i }), n;
 }
+const crosshairPanThresholdCssPx = 4;
+function isCrosshairPanDrag(t, e, n) {
+  const i = e[0] - t[0], r = e[1] - t[1];
+  return i * i + r * r >= n * n;
+}
 function V4(t, e) {
   return e === 0 ? t.model.interaction.primaryDragMode : e === 2 ? t.model.interaction.secondaryDragMode : ge.none;
 }
@@ -32776,6 +32853,10 @@ function $4(t) {
 }
 function Sl(t) {
   const e = t._activeDragMode;
+  if (e === ge.crosshairPan && !t._crosshairPanDidDrag) {
+    const n = Je(t, t.dragEndXY[0], t.dragEndXY[1]);
+    n && t.setCrosshairPos(n);
+  }
   if (e === ge.angle) {
     if (t._angleState === "drawing_first_line") {
       t._angleFirstLine = [
@@ -32849,7 +32930,7 @@ function Al(t) {
   e && t.emit("dragRelease", e);
 }
 function Ml(t) {
-  t._activeDragMode = ge.none, t._pan2DxyzmmAtDragStart = null, t.model._dragOverlay = null, t.drawScene();
+  t._activeDragMode = ge.none, t._pan2DxyzmmAtDragStart = null, t._crosshairPanDidDrag = !1, t.model._dragOverlay = null, t.drawScene();
 }
 function X4(t) {
   const e = t._pan2DxyzmmAtDragStart;
@@ -32859,7 +32940,7 @@ function X4(t) {
     t.dragStartXY[0],
     t.dragStartXY[1]
   ), i = Je(t, t.dragEndXY[0], t.dragEndXY[1]);
-  !n || !i || (t.model.scene.pan2Dxyzmm[0] = e[0] + (i[0] - n[0]), t.model.scene.pan2Dxyzmm[1] = e[1] + (i[1] - n[1]), t.model.scene.pan2Dxyzmm[2] = e[2] + (i[2] - n[2]));
+  !n || !i || (t.model.scene.pan2Dxyzmm[0] = e[0] + (i[0] - n[0]), t.model.scene.pan2Dxyzmm[1] = e[1] + (i[1] - n[1]), t.model.scene.pan2Dxyzmm[2] = e[2] + (i[2] - n[2]), t.emit("change", { property: "pan2Dxyzmm", value: t.model.scene.pan2Dxyzmm }));
 }
 function W4(t) {
   const e = t._pan2DxyzmmAtDragStart;
@@ -33796,7 +33877,7 @@ function k2(t) {
       }
       if (t.isDragging = !0, t.activeButton = n.button, t.activeTileHit && !t.activeTileHit.isRender) {
         const l = V4(t, n.button);
-        if (t._activeDragMode = l, t.dragStartXY = [r, s], t.dragEndXY = [r, s], t.model._dragOverlay = null, l !== ge.angle && (t._angleState = "none"), l === ge.crosshair) {
+        if (t._activeDragMode = l, t._crosshairPanDidDrag = !1, t.dragStartXY = [r, s], t.dragEndXY = [r, s], t.model._dragOverlay = null, l !== ge.angle && (t._angleState = "none"), l === ge.crosshair) {
           const c = at(
             t.view?.screenSlices ?? [],
             t.model,
@@ -33805,7 +33886,7 @@ function k2(t) {
             t.activeTileHit
           );
           c && t.setCrosshairPos(c);
-        } else if (l === ge.pan || l === ge.slicer3D) {
+        } else if (l === ge.pan || l === ge.slicer3D || l === ge.crosshairPan) {
           const c = t.model.scene.pan2Dxyzmm;
           t._pan2DxyzmmAtDragStart = [c[0], c[1], c[2], c[3]];
         } else l === ge.angle && t._angleState !== "drawing_second_line" && (t._angleState = "drawing_first_line");
@@ -33973,7 +34054,7 @@ function k2(t) {
       button: n.button
     });
   }, t._eventListeners.pointercancel = (e) => {
-    t._activeDragMode !== ge.none && Sl(t);
+    t._activeDragMode !== ge.none && (t._activeDragMode === ge.crosshairPan && (t._crosshairPanDidDrag = !0), Sl(t));
     try {
       t._annotation3DActive && Fl(t);
     } finally {
@@ -34295,6 +34376,11 @@ function k2(t) {
         case ge.pan:
           X4(t), t.drawScene();
           break;
+        case ge.crosshairPan: {
+          const c = t.canvas, h = c ? c.width / Math.max(c.getBoundingClientRect().width, 1) : 1, d = crosshairPanThresholdCssPx * h;
+          (t._crosshairPanDidDrag || isCrosshairPanDrag(t.dragStartXY, t.dragEndXY, d)) && (t._crosshairPanDidDrag = !0, X4(t), t.drawScene());
+          break;
+        }
         case ge.slicer3D:
           W4(t), t.drawScene();
           break;
@@ -37046,30 +37132,53 @@ class li extends EventTarget {
   }
   visibleTiles(e) {
     const n = this.selectLevel();
-    if (!n) return { level: null, tiles: [] };
-    const i = e.devicePixelRatio ?? 1, r = this.viewport.centerX - e.widthCss / (2 * this.viewport.scale), s = this.viewport.centerY - e.heightCss / (2 * this.viewport.scale), o = this.viewport.centerX + e.widthCss / (2 * this.viewport.scale), a = this.viewport.centerY + e.heightCss / (2 * this.viewport.scale), l = n.width > 0 ? this.manifest.width / n.width : n.downsample, c = n.height > 0 ? this.manifest.height / n.height : n.downsample, h = Math.floor(r / l), d = Math.floor(s / c), u = Math.ceil(o / l), f = Math.ceil(a / c), m = this.levelTileWidth(n), x = this.levelTileHeight(n), p = Kt(
+    if (!n) return { level: null, tiles: [], fallback: [] };
+    const i = this.tilesForLevel(n, e);
+    return { level: n, tiles: i, fallback: this.fallbackTiles(n, i, e) };
+  }
+  fallbackTiles(e, n, i) {
+    let r = !1;
+    for (const l of n)
+      if (!this._cache.has(l.key)) {
+        r = !0;
+        break;
+      }
+    if (!r) return [];
+    const s = this.manifest.levels, o = s.indexOf(e);
+    if (o < 0) return [];
+    const a = [];
+    for (let l = s.length - 1; l > o; l--) {
+      const c = s[l];
+      if (c)
+        for (const h of this.tilesForLevel(c, i))
+          this._cache.has(h.key) && a.push(h);
+    }
+    return a;
+  }
+  tilesForLevel(e, n) {
+    const i = n.devicePixelRatio ?? 1, r = this.viewport.centerX - n.widthCss / (2 * this.viewport.scale), s = this.viewport.centerY - n.heightCss / (2 * this.viewport.scale), o = this.viewport.centerX + n.widthCss / (2 * this.viewport.scale), a = this.viewport.centerY + n.heightCss / (2 * this.viewport.scale), l = e.width > 0 ? this.manifest.width / e.width : e.downsample, c = e.height > 0 ? this.manifest.height / e.height : e.downsample, h = Math.floor(r / l), d = Math.floor(s / c), u = Math.ceil(o / l), f = Math.ceil(a / c), m = this.levelTileWidth(e), x = this.levelTileHeight(e), p = Kt(
       Math.floor(h / m),
       0,
-      n.columns - 1
-    ), y = Kt(Math.floor(d / x), 0, n.rows - 1), T = Kt(
+      e.columns - 1
+    ), y = Kt(Math.floor(d / x), 0, e.rows - 1), T = Kt(
       Math.floor((u - 1) / m),
       0,
-      n.columns - 1
+      e.columns - 1
     ), _ = Kt(
       Math.floor((f - 1) / x),
       0,
-      n.rows - 1
+      e.rows - 1
     ), b = this.viewport.scale * i, g = [];
     for (let v = y; v <= _; v++)
       for (let w = p; w <= T; w++) {
-        const E = this.tileAt(n, w, v);
+        const E = this.tileAt(e, w, v);
         if (!E) continue;
         const A = E.x * m * l, M = E.y * x * c, P = E.width * l, D = E.height * c, I = this.isYAxisUp() ? (a - (M + D)) * b : (M - s) * b;
         g.push({
           slide: this,
-          level: n,
+          level: e,
           tile: E,
-          key: this.tileKey(n, E),
+          key: this.tileKey(e, E),
           screenX: (A - r) * b,
           screenY: I,
           screenWidth: P * b,
@@ -37077,7 +37186,7 @@ class li extends EventTarget {
           flipY: this.isYAxisUp()
         });
       }
-    return { level: n, tiles: g };
+    return g;
   }
   requestVisibleTiles(e) {
     const n = this.visibleTiles(e);
@@ -37403,9 +37512,9 @@ function MR(t, e) {
   const i = G.create();
   return G.transformMat4(i, G.fromValues(e[0], e[1], e[2]), n), [Ss(i[0]), Ss(i[1]), Ss(i[2])];
 }
-function RR(t, e, n, i) {
-  const r = t.levels.map((o) => o.shape), s = AR(r[0], e, i.cellEdge);
-  return yS(r, { center: s, radius: n }, i.deviceLimit, {
+function RR(t, e, n, i, r = []) {
+  const s = t.levels.map((a) => a.shape), o = AR(s[0], e, i.cellEdge), a = [0, 1, 2].map((l) => Math.max(0, Math.min(s[0][l] - 1e-3, e[l] * s[0][l])));
+  return yS(s, { center: o, reserveCenter: a, reserveBounds: r, radius: n }, i.deviceLimit, {
     cellEdge: i.cellEdge,
     haloSize: i.halo,
     detail: i.detail,
@@ -37429,33 +37538,47 @@ async function DR(t, e) {
   throw n;
 }
 function CR(t, e) {
-  const n = Math.max(1, Math.floor(e.maxConcurrentLoads) || 1), i = Math.max(1, Math.floor(e.retryAttempts) || 1), r = /* @__PURE__ */ new Map();
+  const n = Math.max(1, Math.floor(Number.isFinite(e.maxConcurrentLoads) ? e.maxConcurrentLoads : 1) || 1), i = Math.max(1, Math.floor(Number.isFinite(e.retryAttempts) ? e.retryAttempts : 1) || 1), r = /* @__PURE__ */ new Map();
   let s = 0;
   const o = [], a = () => s < n ? (s++, Promise.resolve()) : new Promise((c) => o.push(c)), l = () => {
     const c = o.shift();
     c ? c() : s--;
   };
   return (c) => {
-    const h = c.desc.sourceLevel ?? 0, d = c.desc.texOrigin, u = c.desc.texDims, f = `${h}|${d.join(",")}|${u.join(",")}`, m = r.get(f);
-    if (m) return m;
-    const x = a().then(
+    const h = c.desc.sourceLevel ?? 0, d = c.desc.texOrigin, u = c.desc.texDims, f = c.signal, m = `${h}|${d.join(",")}|${u.join(",")}`, x = r.get(m);
+    if (x)
+      return x.waiters++, IRa(x, f), x.promise;
+    const p = new AbortController(), y = a().then(
       () => DR(
         () => t.fetchChunk({
           levelIndex: h,
           texOrigin: d,
           texDims: u,
-          bytesPerVoxel: c.bytesPerVoxel
+          bytesPerVoxel: c.bytesPerVoxel,
+          signal: p.signal
         }),
         i
       )
     ).finally(() => l());
-    r.set(f, x);
-    const p = () => {
-      r.get(f) === x && r.delete(f);
+    const T = { promise: y, controller: p, waiters: 1 };
+    r.set(m, T), IRa(T, f);
+    const _ = () => {
+      r.get(m) === T && r.delete(m);
     };
-    return x.then(p, p), x;
+    return y.then(_, _), y;
   };
 }
+function IRa(t, e) {
+  if (!e) return;
+  const n = () => {
+    t.waiters--, t.waiters <= 0 && t.controller.abort(e.reason);
+  };
+  if (e.aborted) {
+    n();
+    return;
+  }
+  e.addEventListener("abort", n, { once: !0 });
+}
 class IR {
   constructor(e, n, i = {}) {
     if (this.disposed = !1, this.refocusHandle = null, this.swapChain = Promise.resolve(), n.levels.length < 1)
@@ -37470,7 +37593,7 @@ class IR {
       deviceLimit: i.deviceLimit ?? LR(e) ?? 256,
       renderCentering: i.renderCentering ?? "none",
       debounceMs: i.debounceMs ?? 150
-    }, this.radiusOpt = i.radius ?? "auto";
+    }, this.radiusOpt = i.radius ?? "auto", this.focusBounds = i.focusBounds?.map((r) => ({ min: [...r.min], max: [...r.max] })) ?? [];
     const r = i.focus ?? "crosshair";
     this.followCrosshair = r === "crosshair", this.focusFrac = Array.isArray(r) ? [r[0], r[1], r[2]] : [0.5, 0.5, 0.5], this.onLocationChange = () => this.handleLocationChange();
     const s = n.levels[0];
@@ -37515,8 +37638,8 @@ class IR {
     return this.plan;
   }
   /** Move the focus and rebuild+swap the plan (debounced). */
-  setFocus(e) {
-    this.focusFrac = [e[0], e[1], e[2]], this.refocus();
+  setFocus(e, n) {
+    this.focusFrac = [e[0], e[1], e[2]], n !== void 0 && (this.focusBounds = n.map((i) => ({ min: [...i.min], max: [...i.max] }))), this.refocus();
   }
   /** Cap the finest level the octree may use (index into `source.levels`). */
   setMaxDetail(e) {
@@ -37551,7 +37674,8 @@ class IR {
       this.source,
       this.focusFrac,
       this.currentRadius(),
-      this.o
+      this.o,
+      this.focusBounds
     );
   }
   currentRadius() {
@@ -37700,7 +37824,7 @@ function $R(t, e) {
 }
 let fD = class extends EventTarget {
   constructor(e = {}, n, i = "both") {
-    super(), this.canvas = null, this._isDraggingFlag = !1, this.view = null, this._settingsSavePolicy = {}, this._slidePlane = null, this._slidePlaneSlide = null, this._slidePlaneOnChange = null, this._slideDrawing = null, this._slideVector = null, this._slideLastRasterPt = null, this._slideTool = "pen", this._slideWandTolerance = 40, this._slideFillPts = [], this._slideVecPts = [], this._dprMediaQuery = null, this._updating = !1, this._pendingUpdate = !1, this._deferredReloads = /* @__PURE__ */ new Set(), this._destroyed = !1, this._deferredVolumes = null, this._deferredMeshes = null, this._drawPenLocation = [NaN, NaN, NaN], this._drawPenAxCorSag = -1, this._drawPenFillPts = [], this._draw3DActive = !1, this._draw3DLastVoxel = null, this._draw3DLastChunk = null, this._draw3DNeedsUndo = !1, this._draw3DSampleCache = null, this._drawLut = null, this._drawingDirty = !1, this._drawDirtyBox = null, this.drawPenAutoClose = !1, this.drawPenFilled = !1, this.drawUndoBitmaps = [], this.currentDrawUndoBitmap = -1, this.maxDrawUndoBitmaps = 8, this.dragStartXY = [0, 0], this.dragEndXY = [0, 0], this._activeDragMode = ge.none, this._angleState = "none", this._angleFirstLine = [0, 0, 0, 0], this._pan2DxyzmmAtDragStart = null, this._annotationUndoStack = new k4(), this._annotationBrushPath = [], this._frozenLoopPoints = null, this._annotationSliceType = 0, this._annotationSlicePosition = 0, this._annotationAnchorMM = [0, 0, 0], this._annotation3DActive = !1, this._annotation3DMMPath = [], this._annotation3DFace = null, this._annotationShapeStart = null, this._resizingControlPoint = -1, this._resizeOriginalShape = null, this._resizingAnnotation = null, this._syncTargets = [], this._syncOpts = {}, this._syncDirty = !1, this._rafId = null, this._perf = null, V.setLogLevel(e.logLevel ?? "info"), this.activeClipPlaneIndex = 0, this.currentClipPlaneIndex = 0;
+    super(), this.canvas = null, this._isDraggingFlag = !1, this.view = null, this._settingsSavePolicy = {}, this._slidePlane = null, this._slidePlaneSlide = null, this._slidePlaneOnChange = null, this._slideDrawing = null, this._slideVector = null, this._slideLastRasterPt = null, this._slideTool = "pen", this._slideWandTolerance = 40, this._slideFillPts = [], this._slideVecPts = [], this._dprMediaQuery = null, this._updating = !1, this._pendingUpdate = !1, this._deferredReloads = /* @__PURE__ */ new Set(), this._destroyed = !1, this._deferredVolumes = null, this._deferredMeshes = null, this._drawPenLocation = [NaN, NaN, NaN], this._drawPenAxCorSag = -1, this._drawPenFillPts = [], this._draw3DActive = !1, this._draw3DLastVoxel = null, this._draw3DLastChunk = null, this._draw3DNeedsUndo = !1, this._draw3DSampleCache = null, this._drawLut = null, this._drawingDirty = !1, this._drawDirtyBox = null, this.drawPenAutoClose = !1, this.drawPenFilled = !1, this.drawUndoBitmaps = [], this.currentDrawUndoBitmap = -1, this.maxDrawUndoBitmaps = 8, this.dragStartXY = [0, 0], this.dragEndXY = [0, 0], this._activeDragMode = ge.none, this._angleState = "none", this._angleFirstLine = [0, 0, 0, 0], this._pan2DxyzmmAtDragStart = null, this._crosshairPanDidDrag = !1, this._annotationUndoStack = new k4(), this._annotationBrushPath = [], this._frozenLoopPoints = null, this._annotationSliceType = 0, this._annotationSlicePosition = 0, this._annotationAnchorMM = [0, 0, 0], this._annotation3DActive = !1, this._annotation3DMMPath = [], this._annotation3DFace = null, this._annotationShapeStart = null, this._resizingControlPoint = -1, this._resizeOriginalShape = null, this._resizingAnnotation = null, this._syncTargets = [], this._syncOpts = {}, this._syncDirty = !1, this._rafId = null, this._perf = null, V.setLogLevel(e.logLevel ?? "info"), this.activeClipPlaneIndex = 0, this.currentClipPlaneIndex = 0;
     const r = e.backend ?? "webgpu";
     if (this.opts = {
       backend: r,
@@ -40186,6 +40310,7 @@ let fD = class extends EventTarget {
         roiSelection: ge.roiSelection,
         angle: ge.angle,
         crosshair: ge.crosshair,
+        crosshairPan: ge.crosshairPan,
         windowing: ge.windowing
       }[e];
       i !== void 0 ? this.model.interaction.secondaryDragMode = i : V.warn(`Unknown drag mode: ${e}`);
diff --git a/dist/NVControlBase.d.ts b/dist/NVControlBase.d.ts
index e06d564ce9e6464e8d161031563abafcc8deb0c3..a57460dad2dce1da5a513117e35da9440d14aa2a 100644
--- a/dist/NVControlBase.d.ts
+++ b/dist/NVControlBase.d.ts
@@ -172,6 +172,7 @@ export default class NiiVue extends EventTarget {
     _angleState: 'none' | 'drawing_first_line' | 'drawing_second_line';
     _angleFirstLine: number[];
     _pan2DxyzmmAtDragStart: [number, number, number, number] | null;
+    _crosshairPanDidDrag: boolean;
     _annotationUndoStack: AnnotationUndoStack;
     _annotationBrushPath: AnnotationPoint[];
     _frozenLoopPoints: AnnotationPoint[] | null;
diff --git a/dist/NVTypes.d.ts b/dist/NVTypes.d.ts
index 56c9890990fee1d0fa1124c617b4805e2fcde883..b1618ef468e311dc1df36f873cdd5445a24c7638 100644
--- a/dist/NVTypes.d.ts
+++ b/dist/NVTypes.d.ts
@@ -10,6 +10,8 @@ export interface VolumeChunkSourceRequest {
     plan: ChunkPlan;
     datatypeCode: number;
     bytesPerVoxel: number;
+    /** Aborts when the renderer no longer needs this chunk. */
+    signal?: AbortSignal;
 }
 export type VolumeChunkSource = (request: VolumeChunkSourceRequest) => ArrayBuffer | Uint8Array | TypedVoxelArray | Promise<ArrayBuffer | Uint8Array | TypedVoxelArray>;
 export interface VolumeChunkExplode {
@@ -518,6 +520,8 @@ export type CustomLayoutTile = {
     sliceType: number;
     position: [number, number, number, number];
     sliceMM?: number;
+    /** Crop the in-plane field of view to this fraction of the shorter extent. */
+    squareCropFraction?: number;
 };
 export type LayoutConfig = {
     sliceType: number;
diff --git a/dist/index.d.ts b/dist/index.d.ts
index 5969cdb8c613b57598252135179af9fb5795c0aa..3bef44f9ba1ae5fbe94c122c1e71c7f93c8d9a78 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -30,7 +30,7 @@ export type { SlideVectorKind, SlideVectorShape } from './slide/slideVector';
 export { SlideVectorLayer } from './slide/slideVector';
 export { buildDrawingLut, drawingBitmapToRGBA } from './view/NVDrawingTexture';
 export type { ChunkedVolumeFetch, ChunkedVolumeLevel, ChunkedVolumeSource, } from './volume/ChunkedVolumeSource';
-export type { ChunkPlan, MultiLodFocus, MultiLodOptions, Vec3f, Vec3i, VolumeChunkDesc, } from './volume/chunking';
+export type { ChunkPlan, MultiLodBounds, MultiLodFocus, MultiLodOptions, Vec3f, Vec3i, VolumeChunkDesc, } from './volume/chunking';
 export { chunkVolumeGrid, chunkVolumeMultiLOD } from './volume/chunking';
 export { buildDerivedScalarVolume, isMrsiVolume } from './volume/mrsi';
 export { type ChunkedVolumeOptions, NVChunkedVolume, } from './volume/NVChunkedVolume';
diff --git a/dist/index.webgl2.d.ts b/dist/index.webgl2.d.ts
index 306a846dede1c51d7d7fcd942dc27152ce06f00f..24bf1289e7da429fa1f293c1cdbf0b09c20234fd 100644
--- a/dist/index.webgl2.d.ts
+++ b/dist/index.webgl2.d.ts
@@ -13,7 +13,7 @@ export type { AffineMatrix, AffineTransform, BackendType, CanvasViewport, ColorM
 export type { SignalFromUrlOptions } from './signal/NVSignal';
 export { apodize, deriveSpectroscopySeries, GYRO_MAG_RATIO, halveFirstPoint, integratePpmBandMap, PPM_RANGE, PPM_SHIFT, type PpmBandOptions, phaseCorrection, ppmRefForNucleus, } from './signal/processing';
 export type { ChunkedVolumeFetch, ChunkedVolumeLevel, ChunkedVolumeSource, } from './volume/ChunkedVolumeSource';
-export type { ChunkPlan, MultiLodFocus, MultiLodOptions, Vec3f, Vec3i, VolumeChunkDesc, } from './volume/chunking';
+export type { ChunkPlan, MultiLodBounds, MultiLodFocus, MultiLodOptions, Vec3f, Vec3i, VolumeChunkDesc, } from './volume/chunking';
 export { chunkVolumeGrid, chunkVolumeMultiLOD } from './volume/chunking';
 export { buildDerivedScalarVolume, isMrsiVolume } from './volume/mrsi';
 export { type ChunkedVolumeOptions, NVChunkedVolume, } from './volume/NVChunkedVolume';
diff --git a/dist/index.webgpu.d.ts b/dist/index.webgpu.d.ts
index 0bb2e42332096615a1d7d8db94f868c9e6d5aeae..e7616233b9cdc945125eb207c38b1d103f13e991 100644
--- a/dist/index.webgpu.d.ts
+++ b/dist/index.webgpu.d.ts
@@ -13,7 +13,7 @@ export type { AffineMatrix, AffineTransform, BackendType, CanvasViewport, ColorM
 export type { SignalFromUrlOptions } from './signal/NVSignal';
 export { apodize, deriveSpectroscopySeries, GYRO_MAG_RATIO, halveFirstPoint, integratePpmBandMap, PPM_RANGE, PPM_SHIFT, type PpmBandOptions, phaseCorrection, ppmRefForNucleus, } from './signal/processing';
 export type { ChunkedVolumeFetch, ChunkedVolumeLevel, ChunkedVolumeSource, } from './volume/ChunkedVolumeSource';
-export type { ChunkPlan, MultiLodFocus, MultiLodOptions, Vec3f, Vec3i, VolumeChunkDesc, } from './volume/chunking';
+export type { ChunkPlan, MultiLodBounds, MultiLodFocus, MultiLodOptions, Vec3f, Vec3i, VolumeChunkDesc, } from './volume/chunking';
 export { chunkVolumeGrid, chunkVolumeMultiLOD } from './volume/chunking';
 export { buildDerivedScalarVolume, isMrsiVolume } from './volume/mrsi';
 export { type ChunkedVolumeOptions, NVChunkedVolume, } from './volume/NVChunkedVolume';
diff --git a/dist/niivue.js b/dist/niivue.js
index c6e51089858833f9fcce35db580589ca5562641a..bbf7a002fa3f51e31909e594107e0c594fb99328 100644
--- a/dist/niivue.js
+++ b/dist/niivue.js
@@ -82,8 +82,34 @@ class N extends b {
         !1
       );
       const n = s.requestVisibleTiles(i);
+      for (const h of n.fallback) {
+        const d = s.cachedTileBitmap(h.key);
+        if (!d) continue;
+        const a = this.textureForBitmap(e, h.key, d);
+        if (!a) continue;
+        this.drawTile(
+          e,
+          r,
+          o,
+          {
+            x: h.screenX,
+            y: h.screenY,
+            width: h.screenWidth,
+            height: h.screenHeight
+          },
+          a.texture,
+          s.placeholderColor,
+          s.gridColor,
+          s.opacity,
+          h,
+          !1,
+          !1
+        );
+      }
       for (const h of n.tiles) {
-        const d = s.cachedTileBitmap(h.key), a = d ? this.textureForBitmap(e, h.key, d) : null, c = {
+        const d = s.cachedTileBitmap(h.key);
+        if (!d && n.fallback.length > 0) continue;
+        const a = d ? this.textureForBitmap(e, h.key, d) : null, c = {
           x: h.screenX,
           y: h.screenY,
           width: h.screenWidth,
@@ -460,8 +486,37 @@ class R {
         gridColor: n.gridColor
       });
       const d = n.requestVisibleTiles(t);
+      for (const a of d.fallback) {
+        const c = n.cachedTileBitmap(a.key);
+        if (!c) continue;
+        const l = this.textureForBitmap(a.key, c);
+        if (!l) continue;
+        this.drawQuad(
+          u,
+          r,
+          o,
+          {
+            x: a.screenX,
+            y: a.screenY,
+            width: a.screenWidth,
+            height: a.screenHeight
+          },
+          l.texture,
+          {
+            uvTop: a.flipY ? 1 : 0,
+            uvBottom: a.flipY ? 0 : 1,
+            opacity: n.opacity,
+            isPlaceholder: !1,
+            showGrid: !1,
+            placeholderColor: n.placeholderColor,
+            gridColor: n.gridColor
+          }
+        );
+      }
       for (const a of d.tiles) {
-        const c = n.cachedTileBitmap(a.key), l = c ? this.textureForBitmap(a.key, c) : null, f = {
+        const c = n.cachedTileBitmap(a.key);
+        if (!c && d.fallback.length > 0) continue;
+        const l = c ? this.textureForBitmap(a.key, c) : null, f = {
           x: a.screenX,
           y: a.screenY,
           width: a.screenWidth,
diff --git a/dist/slide/NVSlide.d.ts b/dist/slide/NVSlide.d.ts
index f629d364ae49ee1b969a7a548e720faf0a7cdcbd..94028ffbe0c6f1e46f421ff752b17c96aec47eee 100644
--- a/dist/slide/NVSlide.d.ts
+++ b/dist/slide/NVSlide.d.ts
@@ -112,6 +112,7 @@ export interface NVSlideVisibleTile {
 export interface NVSlideVisibleTiles {
     level: NVSlideLevelManifest | null;
     tiles: NVSlideVisibleTile[];
+    fallback: NVSlideVisibleTile[];
 }
 export interface NVSlideScreenRect {
     x: number;
@@ -238,6 +239,8 @@ export declare class NVSlide extends EventTarget {
     dispose(): void;
     private levelTileWidth;
     private levelTileHeight;
+    private fallbackTiles;
+    private tilesForLevel;
     private tileAt;
     private tileKey;
     private resolveUrl;
diff --git a/dist/view/NVRuler.d.ts b/dist/view/NVRuler.d.ts
index d38eac8cbc65f11cfaf87bddd594f72e0076e95d..5233235c5685bf63e7d28291f22a56194b5e9c26 100644
--- a/dist/view/NVRuler.d.ts
+++ b/dist/view/NVRuler.d.ts
@@ -6,7 +6,7 @@ export type RulerResult = {
     labels: GlyphBatch[];
 };
 /**
- * Build a checkerboard scale ruler on the best 2D tile.
+ * Build a simple horizontal scale ruler on the best 2D tile.
  * Returns lines and labels in canvas-pixel coordinates, or null if no ruler should be drawn.
  */
-export declare function buildRuler(tiles: SliceTile[], buildText: BuildTextFn, buildLine: BuildLineFn, fontColor: number[], _backColor: number[]): RulerResult | null;
+export declare function buildRuler(tiles: SliceTile[], buildText: BuildTextFn, buildLine: BuildLineFn, fontColor: number[], _backColor: number[], zoom?: number, viewportSize?: readonly [number, number]): RulerResult | null;
diff --git a/dist/volume/ChunkedVolumeSource.d.ts b/dist/volume/ChunkedVolumeSource.d.ts
index fb7c62abff3f5115eabcef927dc9f35daf7e1f8e..34aec9aeaf6582e6d4eff47b59834520e17d6f3e 100644
--- a/dist/volume/ChunkedVolumeSource.d.ts
+++ b/dist/volume/ChunkedVolumeSource.d.ts
@@ -18,6 +18,8 @@ export interface ChunkedVolumeFetch {
     texDims: Vec3i;
     /** Bytes per voxel for {@link ChunkedVolumeSource.datatypeCode}. */
     bytesPerVoxel: number;
+    /** Aborts when the renderer no longer needs this region. */
+    signal?: AbortSignal;
 }
 /**
  * Pluggable, format-agnostic source for a multi-resolution (pyramid) volume.
diff --git a/dist/volume/NVChunkedVolume.d.ts b/dist/volume/NVChunkedVolume.d.ts
index 4f62bcb330bb08c9fde315f00bfcbec9ecfe4546..1ca14ce8a133f1ec75a61bc0aa71699e7e598630 100644
--- a/dist/volume/NVChunkedVolume.d.ts
+++ b/dist/volume/NVChunkedVolume.d.ts
@@ -2,7 +2,7 @@ import { mat4 } from 'gl-matrix';
 import { default as NiiVue } from '../NVControlBase';
 import { NVImage, VolumeChunkSource } from '../NVTypes';
 import { ChunkedVolumeSource } from './ChunkedVolumeSource';
-import { ChunkPlan, Vec3f, Vec3i } from './chunking';
+import { ChunkPlan, MultiLodBounds, Vec3f, Vec3i } from './chunking';
 /** Options for {@link NiiVue.loadChunkedVolume}. */
 export interface ChunkedVolumeOptions {
     /** Display window minimum (default 0). */
@@ -24,6 +24,8 @@ export interface ChunkedVolumeOptions {
      * fraction pins a static focus.
      */
     focus?: 'crosshair' | Vec3f;
+    /** Visible bounds that must be assigned one uniform pyramid level. */
+    focusBounds?: MultiLodBounds[];
     /**
      * Finest-LOD radius in common-grid voxels. `'auto'` (default) derives it from
      * the view: tight in the 3D render view, the visible-slice box in multiplanar
@@ -88,7 +90,7 @@ export declare function focusCenterBiased(common: Vec3i, frac: Vec3f, cellEdge:
  */
 export declare function mmToVolumeFraction(frac2mm: mat4, mm: Vec3f): Vec3f | null;
 /** Build a crosshair-focused multi-LOD plan for a source at a focus + radius. */
-export declare function planForFocus(source: ChunkedVolumeSource, focusFrac: Vec3f, radius: number, o: ResolvedOptions): ChunkPlan;
+export declare function planForFocus(source: ChunkedVolumeSource, focusFrac: Vec3f, radius: number, o: ResolvedOptions, focusBounds?: MultiLodBounds[]): ChunkPlan;
 /**
  * Wrap a {@link ChunkedVolumeSource} as a renderer `VolumeChunkSource`: dispatch
  * each brick to its own pyramid level (`desc.sourceLevel`), bound in-flight
@@ -118,6 +120,7 @@ export declare class NVChunkedVolume {
     private readonly radiusOpt;
     private readonly onLocationChange;
     private focusFrac;
+    private focusBounds;
     private plan;
     private disposed;
     private refocusHandle;
@@ -136,7 +139,7 @@ export declare class NVChunkedVolume {
     /** The current multi-LOD plan (read-only; useful for debugging/telemetry). */
     get currentPlan(): ChunkPlan;
     /** Move the focus and rebuild+swap the plan (debounced). */
-    setFocus(frac: Vec3f): void;
+    setFocus(frac: Vec3f, focusBounds?: MultiLodBounds[]): void;
     /** Cap the finest level the octree may use (index into `source.levels`). */
     setMaxDetail(levelIndex: number): void;
     /** Change the plan's GPU byte budget. */
diff --git a/dist/volume/chunking.d.ts b/dist/volume/chunking.d.ts
index 34e9f0dcdd17157e8e544c872a023c495f4f2e4d..6004ca2b596fe75c8024afc76afb3c55769501e2 100644
--- a/dist/volume/chunking.d.ts
+++ b/dist/volume/chunking.d.ts
@@ -140,9 +140,25 @@ export declare function chunkAtVoxel(plan: ChunkPlan, voxel: Vec3i): VolumeChunk
  */
 export declare function matchChunksByContent(oldPlan: ChunkPlan, newPlan: ChunkPlan): Map<number, number>;
 /** A focused region of interest, in COMMON (finest, level-0) voxel coordinates. */
+export interface MultiLodBounds {
+    /** Inclusive lower corner in common-grid voxels. */
+    min: Vec3f;
+    /** Exclusive upper corner in common-grid voxels. */
+    max: Vec3f;
+}
 export interface MultiLodFocus {
     /** Focus centre in common-grid voxels. */
     center: Vec3f;
+    /**
+     * Optional un-biased focus used for the mandatory finest-detail branch.
+     * `center` may remain biased to stabilise context subdivision.
+     */
+    reserveCenter?: Vec3f;
+    /**
+     * Visible regions that must use one uniform level. The budget pass may
+     * coarsen that level, but never mixes levels inside these bounds.
+     */
+    reserveBounds?: MultiLodBounds[];
     /**
      * Focus radius in common-grid voxels. Bricks whose region lies within this
      * distance of the centre render at the finest level; each further doubling of
